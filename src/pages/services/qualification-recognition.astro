---
/**
 * Foreign Qualification & Degree Recognition — InterProFinland
 * Comprehensive guidance on OPH, Valvira, and Finnish credential equivalency.
 * Luxury editorial design matching Home and About design systems.
 */

import Layout from '@/layouts/Layout.astro';
import Section from '@/components/ui/Section.astro';
import Container from '@/components/ui/Container.astro';
import CtaSection from '@/components/sections/CtaSection.astro';

/* ── Competent Authorities Directory in Finland ── */
const authorities = [
  {
    code: 'OPH',
    category: 'Higher Education & Teaching',
    name: 'Finnish National Agency for Education (Opetushallitus)',
    scope: 'Evaluates foreign higher education qualifications for equivalence with Finnish university degrees (Bachelor, Master, Doctorate). Grants eligibility decisions for public sector posts, teachers, subject instructors, and early childhood educators.',
    link: 'https://www.oph.fi/en/services/recognition-and-international-comparability-qualifications',
  },
  {
    code: 'Valvira',
    category: 'Healthcare & Social Welfare',
    name: 'National Supervisory Authority for Welfare and Health',
    scope: 'Grants official practice rights, professional registrations, and title protection for regulated healthcare and social welfare practitioners (doctors, nurses, dentists, pharmacists, psychologists, social workers).',
    link: 'https://valvira.fi/en/rights-to-practise',
  },
  {
    code: 'Ruokavirasto',
    category: 'Veterinary & Food Safety',
    name: 'Finnish Food Authority',
    scope: 'Approves qualifications and issues official practice licenses for veterinarians, animal health practitioners, and food safety inspectors qualifying outside the EU/EEA.',
    link: 'https://www.ruokavirasto.fi/en/',
  },
  {
    code: 'Traficom',
    category: 'Transport & Maritime',
    name: 'Finnish Transport and Communications Agency',
    scope: 'Validates professional qualifications, maritime captain licenses, aviation pilot credentials, and specialized transport engineering endorsements.',
    link: 'https://www.traficom.fi/en/',
  },
];

/* ── 4-Stage Recognition Roadmap ── */
const roadmapStages = [
  {
    stage: '01',
    title: 'Determine Authority & Pathway',
    description:
      'We help you identify whether your profession is regulated or non-regulated in Finland, determining whether you need an academic comparability statement (OPH) or formal licensure from a specific agency (Valvira).',
    finnishTerm: 'Toimivaltainen viranomainen',
  },
  {
    stage: '02',
    title: 'Assemble Dossier & Sworn Translations',
    description:
      'Compiling your degree certificate, Diploma Supplement, and complete transcripts. Arranging certified translations prepared by an authorized translator when documents are in non-official languages.',
    finnishTerm: 'Auktorisoitu kääntäjä',
  },
  {
    stage: '03',
    title: 'Submit Application & Track Progress',
    description:
      'Submitting your formal dossier to the relevant authority, monitoring progress, managing verification requests, and preparing any supplementary clarifications if requested.',
    finnishTerm: 'Hakemuksen käsittely',
  },
  {
    stage: '04',
    title: 'Adaptation Period or Full Practice Rights',
    description:
      'For regulated professions requiring conditional decisions, navigating required aptitude tests, supervised adaptation periods, or supplementary university modules to achieve full licensure.',
    finnishTerm: 'Kelpoisuuskoe / Sopeutumisaika',
  },
];

/* ── Essential Document Requirements ── */
const documentRequirements = [
  {
    title: 'Official Degree Certificate & Diploma',
    detail: 'Original certificate in the native language plus certified English, Finnish, or Swedish translation.',
  },
  {
    title: 'Official Academic Transcript (Diploma Supplement)',
    detail: 'Comprehensive record of completed courses, study hours, credits (ECTS equivalent), and grading scale description.',
  },
  {
    title: 'Professional License / Certificate of Good Standing',
    detail: 'Required for regulated healthcare and legal professions to confirm active, unencumbered practice rights in your home country.',
  },
  {
    title: 'Valid Passport or Official Identity Document',
    detail: 'Official identification verifying personal identity, nationality, and right of residence in Finland.',
  },
];

/* ── Specific Recognition FAQs ── */
const recognitionFaqs = [
  {
    q: 'How long does the OPH degree comparability process typically take?',
    a: 'Processing times for OPH comparability decisions typically range from 2 to 4 months once all required certified documents have been received by the agency.',
  },
  {
    q: 'Do I need a decision if my profession is non-regulated (e.g. IT or Business)?',
    a: 'For private sector non-regulated roles, employers can hire based on skills without a state decision. However, an OPH statement officially confirms your academic level (e.g., Master’s level) and provides credibility.',
  },
  {
    q: 'What is the difference between an academic statement and professional recognition?',
    a: 'An academic statement compares degree level (e.g., your Bachelor equals a Finnish Kandidaatti). Professional recognition grants the legal right to practice a specific regulated profession in Finland.',
  },
  {
    q: 'How does InterProFinland assist with Valvira or OPH paperwork?',
    a: 'We review your documents for completeness, help clarify authority instructions, advise on certified translation requirements, and connect you with peers who completed the same process. Note that official decisions are issued solely by competent Finnish authorities.',
  },
];
---

<Layout
  title="Foreign Qualification & Degree Recognition — InterProFinland"
  description="Step-by-step guidance on validating foreign degrees, navigating OPH and Valvira regulations, and achieving professional practice rights in Finland."
>
  <!-- ==========================================================================
       HERO SECTION: Purpose Manifesto Pattern (16:9 Realistic Frame)
       ========================================================================== -->
  <Section background="surface">
    <Container size="page">
      <div class="service-hero-layout">
        <div class="service-hero-content">
          <div class="service-hero-breadcrumb">
            <a href="/services" class="breadcrumb-back">← Back to Services</a>
            <span class="breadcrumb-separator">/</span>
            <span class="breadcrumb-current">Qualification Recognition</span>
          </div>

          <h1 class="service-hero-title">
            Foreign Qualification &amp; Degree <span class="service-hero-serif">Recognition in Finland</span>
          </h1>

          <p class="service-hero-lead">
            Navigating Finnish regulatory authorities can be complex. We provide step-by-step guidance
            to help you understand qualification requirements, compile authentic application dossiers, and navigate
            comparability or licensing pathways.
          </p>

          <!-- Key Areas - Desktop -->
          <div class="hero-areas-list" aria-label="Key Recognition Focus Areas">
            <span class="area-item">OPH Comparability Decisions</span>
            <span class="area-divider">•</span>
            <span class="area-item">Valvira Healthcare Licensing</span>
            <span class="area-divider">•</span>
            <span class="area-item">Sworn Translation Guidance</span>
            <span class="area-divider">•</span>
            <span class="area-item">Regulated Profession Rights</span>
          </div>

          <!-- Mobile Image with Overlay Chips -->
          <div class="hero-image-mobile">
            <div class="service-hero-frame">
              <img
                src="/images/services/Qualification-Recognition/recognition-hero.jpg"
                alt="Professional reviewing international degree certificates and academic credentials for Finnish recognition process"
                class="service-hero-img"
                width="800"
                height="450"
                loading="eager"
                decoding="async"
              />
            </div>
            <div class="hero-areas-list-overlay">
              <span class="area-item">OPH Comparability</span>
              <span class="area-item">Valvira Licensing</span>
              <span class="area-item">Sworn Translations</span>
              <span class="area-item">Profession Rights</span>
            </div>
          </div>

          <div class="service-hero-actions">
            <a
              href="https://form.jotform.com/InterProFinland/membership-application-form"
              target="_blank"
              rel="noopener noreferrer"
              class="btn-primary-action"
            >
              <span>Request Recognition Guidance</span>
              <span aria-hidden="true">→</span>
            </a>
            <a href="#roadmap" class="btn-secondary-action">
              <span>Explore 4-Stage Roadmap</span>
            </a>
          </div>
        </div>

        <div class="service-hero-media hero-image-desktop">
          <div class="service-hero-frame">
            <img
              src="/images/services/Qualification-Recognition/recognition-hero.jpg"
              alt="Professional reviewing international degree certificates and academic credentials for Finnish recognition process"
              class="service-hero-img"
              width="800"
              height="450"
              loading="eager"
              decoding="async"
            />
          </div>
        </div>
      </div>
    </Container>
  </Section>

  <!-- ==========================================================================
       AUTHORITIES DIRECTORY: Clean 2×2 Grid Layout
       ========================================================================== -->
  <Section>
    <div id="authorities-directory"></div>
    <Container size="page">
      <div class="section-header-center">
        <span class="section-eyebrow">Regulatory Authorities</span>
        <h2 class="section-heading">Who Evaluates Your Credentials in Finland?</h2>
        <p class="section-intro">
          Different competent authorities handle specific fields. Below are the primary official bodies
          responsible for degree comparability and professional licensing.
        </p>
      </div>

      <div class="authorities-grid">
        {
          authorities.map((auth) => (
            <details class="authority-card" open>
              <summary class="authority-summary">
                <div class="authority-header">
                  <div class="authority-top-row">
                    <span class="authority-code">{auth.code}</span>
                    <span class="authority-category">{auth.category}</span>
                  </div>
                  <h3 class="authority-name">{auth.name}</h3>
                </div>
                <span class="authority-toggle-icon" aria-hidden="true">▼</span>
              </summary>
              <div class="authority-content">
                <p class="authority-scope">{auth.scope}</p>
                <a href={auth.link} target="_blank" rel="noopener noreferrer" class="authority-link">
                  <span>Visit Official Portal</span>
                  <span aria-hidden="true">↗</span>
                </a>
              </div>
            </details>
          ))
        }
      </div>
    </Container>
  </Section>

  <!-- ==========================================================================
       ROADMAP: 2×2 Grid — Clean Process Cards
       ========================================================================== -->
  <Section background="surface">
    <div id="roadmap"></div>
    <Container size="page">
      <div class="section-header-center">
        <span class="section-eyebrow">Step-by-Step Roadmap</span>
        <h2 class="section-heading">The 4 Stages of the Recognition Journey</h2>
        <p class="section-intro">
          A structured sequence ensures all documentation meets Finnish legal standards before formal submission.
        </p>
      </div>

      <div class="roadmap-grid">
        {
          roadmapStages.map((st) => (
            <details class="roadmap-card" open>
              <summary class="roadmap-summary">
                <div class="roadmap-header">
                  <span class="roadmap-number">{st.stage}</span>
                  <h3 class="roadmap-title">{st.title}</h3>
                </div>
                <span class="roadmap-toggle-icon" aria-hidden="true">▼</span>
              </summary>
              <div class="roadmap-content">
                <p class="roadmap-desc">{st.description}</p>
                <div class="roadmap-finnish-term">
                  <span class="term-label">Finnish Term:</span>
                  <span class="term-text">{st.finnishTerm}</span>
                </div>
              </div>
            </details>
          ))
        }
      </div>
    </Container>
  </Section>

  <!-- ==========================================================================
       SPLIT EDITORIAL: Document Preparation & Translations (4:3 Media Frame)
       ========================================================================== -->
  <Section>
    <Container size="page">
      <div class="editorial-split-layout">
        <div class="split-content-side">
          <span class="section-eyebrow">Document Checklist</span>
          <h2 class="split-heading">Required Documents &amp; Translation Standards</h2>
          <p class="split-lead">
            Finnish authorities maintain strict standards for document authentication. Having certified
            translations and complete transcripts ready is vital.
          </p>

          <div class="document-disclaimer-note">
            <span class="disclaimer-icon" aria-hidden="true">ℹ</span>
            <p class="disclaimer-text">
              <strong>Important:</strong> Document requirements vary depending on your specific profession,
              country of qualification, and the competent evaluating authority.
            </p>
          </div>

          <div class="requirements-list">
            {
              documentRequirements.map((doc) => (
                <div class="requirement-item">
                  <div class="req-check" aria-hidden="true">✓</div>
                  <div class="req-info">
                    <h4 class="req-title">{doc.title}</h4>
                    <p class="req-detail">{doc.detail}</p>
                  </div>
                </div>
              ))
            }
          </div>
        </div>

        <div class="split-visual-side">
          <div class="split-image-frame">
            <img
              src="/images/services/Qualification-Recognition/1.jfif"
              alt="International degree documents, diplomas, and certified translations required for Finnish qualification recognition"
              class="split-img"
              width="800"
              height="600"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>
    </Container>
  </Section>

  <!-- ==========================================================================
       HANDBOOK SPOTLIGHT: Qualifications Recognition in Finland Handbook
       ========================================================================== -->
  <Section background="surface">
    <Container size="page">
      <div class="handbook-spotlight-card">
        <div class="handbook-content">
          <div class="handbook-badge-row">
            <span class="handbook-pill">Official Member Resource</span>
            <span class="handbook-free-tag">Available Free to Members</span>
          </div>

          <h2 class="handbook-title">Qualifications Recognition in Finland Handbook</h2>
          <p class="handbook-subtitle">
            Your Essential Guide to Navigating Credential Equivalency, Regulated Professions, and Application Steps
          </p>

          <p class="handbook-desc">
            Finland’s qualification recognition process can be intricate, but you don't have to navigate it alone.
            Our comprehensive handbook provides clear, step-by-step instructions designed to fast-track your career.
          </p>

          <div class="handbook-highlights-grid">
            <div class="handbook-item">
              <span class="handbook-check" aria-hidden="true">✓</span>
              <div>
                <strong>System Overview &amp; Authorities:</strong> Clear breakdown of OPH, Valvira, Ruokavirasto, and Traficom procedures.
              </div>
            </div>
            <div class="handbook-item">
              <span class="handbook-check" aria-hidden="true">✓</span>
              <div>
                <strong>Sector-Specific Focus:</strong> In-depth guidance for Healthcare, Social Welfare, Teaching, and Early Childhood Education.
              </div>
            </div>
            <div class="handbook-item">
              <span class="handbook-check" aria-hidden="true">✓</span>
              <div>
                <strong>Application Success Tips:</strong> How to avoid common documentation mistakes, handle translations, and prepare for aptitude tests.
              </div>
            </div>
          </div>

          <div class="handbook-actions">
            <a
              href="https://form.jotform.com/InterProFinland/membership-application-form"
              target="_blank"
              rel="noopener noreferrer"
              class="btn-handbook-primary"
            >
              <span>Get the Handbook via Membership</span>
              <span aria-hidden="true">→</span>
            </a>
          </div>
        </div>

        <div class="handbook-visual">
          <div class="handbook-media-frame">
            <img
              src="/images/home/handbook-cover.webp"
              alt="Qualifications Recognition in Finland Handbook cover"
              class="handbook-cover-img"
              width="800"
              height="600"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>
    </Container>
  </Section>

  <!-- ==========================================================================
       SPECIFIC RECOGNITION FAQS
       ========================================================================== -->
  <Section>
    <Container size="page">
      <div class="section-header-center">
        <span class="section-eyebrow">Recognition FAQs</span>
        <h2 class="section-heading">Frequently Asked Questions</h2>
        <p class="section-intro">
          Direct answers to common questions on foreign degree evaluation in Finland.
        </p>
      </div>

      <div class="faqs-grid">
        {
          recognitionFaqs.map((faq) => (
            <details class="faq-accordion-item">
              <summary class="faq-accordion-trigger">
                <span class="faq-q-text">{faq.q}</span>
                <span class="faq-accordion-icon" aria-hidden="true">
                  <svg
                    width="18"
                    height="18"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                    focusable="false"
                  >
                    <polyline points="6 9 12 15 18 9"></polyline>
                  </svg>
                </span>
              </summary>
              <div class="faq-accordion-body">
                <p class="faq-a-text">{faq.a}</p>
              </div>
            </details>
          ))
        }
      </div>
    </Container>
  </Section>

  <!-- ==========================================================================
       BOTTOM CTA
       ========================================================================== -->
  <CtaSection
    heading="Need Guidance on Your Foreign Degree in Finland?"
    subheading="Book a consultation with our advisory team to review your qualifications, identify competent authorities, and map out your official recognition pathway."
    background="surface"
    cta={{
      label: 'Request Recognition Guidance',
      href: 'https://form.jotform.com/InterProFinland/membership-application-form',
      target: '_blank',
    }}
    secondaryCta={{
      label: 'Explore All Services',
      href: '/services',
    }}
  />
</Layout>

<style>
  .section-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-primary-500);
    margin-bottom: var(--space-3);
    line-height: 1;
  }

  .section-eyebrow::before,
  .section-eyebrow::after {
    content: '';
    display: block;
    width: 28px;
    height: 2.5px;
    background-color: #ffd26f;
    flex-shrink: 0;
  }

  .section-heading {
    font-family: var(--font-display);
    font-size: clamp(1.875rem, 3vw, 2.5rem);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin: 0 0 var(--space-3);
    line-height: 1.2;
    letter-spacing: -0.02em;
    text-align: center;
  }

  .section-intro {
    font-family: var(--font-body);
    font-size: 1rem;
    color: var(--color-text-secondary);
    max-width: 65ch;
    margin: 0 auto var(--space-6);
    line-height: var(--line-height-relaxed);
    text-align: center;
  }

  .section-header-center {
    text-align: center;
    margin-bottom: var(--space-6);
  }

  /* ── Hero Section (16:9 Frame) ── */
  .service-hero-layout {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: clamp(2rem, 4vw, 3.5rem);
    align-items: center;
    padding: clamp(0.5rem, 1.5vw, 1.5rem) 0 clamp(2rem, 3vw, 3rem);
  }

  .service-hero-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-3);
  }

  .service-hero-breadcrumb {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-display);
    font-size: 0.8125rem;
    font-weight: 600;
  }

  .breadcrumb-back {
    color: var(--color-primary-600, #1E3A8A);
    text-decoration: none;
    transition: color 200ms ease;
  }

  .breadcrumb-back:hover {
    color: #131F5B;
    text-decoration: underline;
  }

  .breadcrumb-separator {
    color: var(--color-gray-400);
  }

  .breadcrumb-current {
    color: var(--color-gray-600);
  }

  .service-hero-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 3.5vw, 2.85rem);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    line-height: 1.18;
    letter-spacing: -0.025em;
    margin: 0;
  }

  .service-hero-serif {
    font-family: var(--font-body);
    font-style: italic;
    font-weight: var(--font-weight-regular);
    color: var(--color-primary-600, #1E3A8A);
  }

  .service-hero-lead {
    font-family: var(--font-body);
    font-size: clamp(1.0625rem, 1.4vw, 1.1875rem);
    line-height: var(--line-height-relaxed);
    color: var(--color-gray-800);
    margin: 0;
  }

  /* Key Areas List — Clean & Simple */
  .hero-areas-list {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: var(--color-gray-600);
  }

  .area-item {
    color: var(--color-text-primary);
    font-weight: 500;
  }

  .area-divider {
    color: var(--color-gray-400);
    font-weight: 300;
  }

  .service-hero-chips {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .hero-chip {
    font-family: var(--font-display);
    font-size: 0.78125rem;
    font-weight: 600;
    color: #131F5B;
    background-color: #ffffff;
    border: 1px solid var(--color-border-subtle, #e2e8f0);
    padding: 4px 10px;
    border-radius: var(--radius-sm, 6px);
  }

  .service-hero-actions {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2.5vw, 1.75rem);
    flex-wrap: wrap;
    margin-top: var(--space-2);
  }

  .btn-primary-action {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background-color: var(--color-gold, #FFD26F);
    color: #131F5B;
    border: 2px solid var(--color-gold, #FFD26F);
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 700;
    text-decoration: none;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-sm);
    box-shadow: 0 4px 14px var(--color-gold-shadow, rgba(255, 210, 111, 0.35));
    transition: background-color 200ms ease, color 200ms ease, border-color 200ms ease, transform 150ms ease;
  }

  .btn-primary-action:hover {
    background-color: var(--color-primary-600, #1E3A8A);
    border-color: var(--color-primary-600, #1E3A8A);
    color: #ffffff;
    transform: translateY(-2px);
  }

  .btn-secondary-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    color: #131F5B;
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 600;
    text-decoration: none;
    padding: var(--space-3) var(--space-6);
    border: 2px solid #131F5B;
    background-color: transparent;
    border-radius: var(--radius-sm);
    transition: background-color 200ms ease, color 200ms ease, border-color 200ms ease, transform 150ms ease;
  }

  .btn-secondary-action:hover {
    background-color: #131F5B;
    color: #ffffff;
    transform: translateY(-2px);
  }

  .service-hero-frame {
    position: relative;
    border-radius: var(--radius-lg, 16px);
    overflow: hidden;
    aspect-ratio: 16 / 9;
    box-shadow: 0 12px 32px -8px rgba(19, 31, 91, 0.12),
                0 0 0 1px rgba(19, 31, 91, 0.08);
    background-color: #f8fafc;
  }

  .service-hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 500ms ease;
  }

  .service-hero-frame:hover .service-hero-img {
    transform: scale(1.03);
  }

  /* ── Authorities Grid — 2×2 Clean Layout ── */
  .authorities-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
    max-width: 1200px;
    margin: 0 auto;
  }

  .authority-card {
    background: #ffffff;
    border: 1px solid var(--color-border-subtle, #e2e8f0);
    border-radius: var(--radius-lg, 16px);
    padding: var(--space-6);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.03);
    transition: transform 250ms ease, box-shadow 250ms ease, border-color 250ms ease;
  }

  .authority-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 24px -6px rgba(19, 31, 91, 0.1);
    border-color: #93c5fd;
  }

  .authority-card[open] {
    border-color: #93c5fd;
  }

  /* Desktop: Open by default */
  @media (min-width: 769px) {
    .authority-card {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }

    /* Force open and disable interaction on desktop */
    .authority-card[open] {
      pointer-events: auto;
    }

    .authority-summary {
      display: contents;
      cursor: default;
      list-style: none;
      user-select: auto;
      pointer-events: none;
    }

    .authority-summary::-webkit-details-marker {
      display: none;
    }

    .authority-toggle-icon {
      display: none;
    }

    .authority-content {
      display: contents;
      pointer-events: auto;
    }

    /* Restore pointer events for links */
    .authority-link {
      pointer-events: auto;
    }
  }

  /* Mobile: Accordion behavior */
  @media (max-width: 768px) {
    .authority-summary {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: var(--space-3);
      cursor: pointer;
      list-style: none;
      user-select: none;
    }

    .authority-summary::-webkit-details-marker {
      display: none;
    }

    .authority-toggle-icon {
      display: inline-block;
      font-size: 1rem;
      color: var(--color-primary-600, #1E3A8A);
      flex-shrink: 0;
      transition: transform 250ms ease, color 200ms ease;
      margin-top: 4px;
      line-height: 1;
    }

    .authority-card[open] .authority-toggle-icon {
      transform: rotate(180deg);
      color: var(--color-primary-700, #131F5B);
    }

    .authority-content {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
      padding-top: var(--space-4);
      margin-top: var(--space-3);
      border-top: 1px solid var(--color-border-subtle, #e2e8f0);
    }
  }

  .authority-header {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    flex: 1;
  }

  .authority-top-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    padding-bottom: var(--space-3);
    border-bottom: 2px solid var(--color-border-subtle, #e2e8f0);
  }

  .authority-code {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.125rem;
    color: #ffffff;
    background-color: #1E3A8A;
    padding: 6px 14px;
    border-radius: var(--radius-sm, 6px);
    letter-spacing: 0.02em;
  }

  .authority-category {
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--color-gray-500);
    text-align: right;
    line-height: 1.2;
  }

  .authority-name {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0;
    line-height: 1.35;
  }

  .authority-scope {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--color-gray-700);
    margin: 0;
  }

  .authority-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-primary-600, #1E3A8A);
    text-decoration: none;
    padding-top: var(--space-3);
    border-top: 1px solid var(--color-border-subtle, #f1f5f9);
    transition: color 200ms ease;
  }

  .authority-link:hover {
    color: #131F5B;
    text-decoration: underline;
  }

  /* ── Roadmap Cards — 2×2 Grid with Accordion on Mobile ── */
  .roadmap-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
    max-width: 1200px;
    margin: 0 auto;
  }

  .roadmap-card {
    background: #ffffff;
    border: 1px solid var(--color-border-subtle, #e2e8f0);
    border-radius: var(--radius-lg, 16px);
    padding: var(--space-6);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.03);
    transition: all 300ms ease;
  }

  .roadmap-card:hover {
    border-color: #1E3A8A;
    box-shadow: 0 8px 24px -4px rgba(19, 31, 91, 0.08);
    transform: translateY(-2px);
  }

  .roadmap-card[open] {
    border-color: #1E3A8A;
  }

  /* Desktop: Open by default */
  @media (min-width: 769px) {
    .roadmap-card {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    /* Force open and disable interaction on desktop */
    .roadmap-card[open] {
      pointer-events: auto;
    }

    .roadmap-summary {
      display: contents;
      cursor: default;
      list-style: none;
      user-select: auto;
      pointer-events: none;
    }

    .roadmap-summary::-webkit-details-marker {
      display: none;
    }

    .roadmap-toggle-icon {
      display: none;
    }

    .roadmap-content {
      display: contents;
      pointer-events: auto;
    }
  }

  /* Mobile: Accordion behavior */
  @media (max-width: 768px) {
    .roadmap-summary {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: var(--space-3);
      cursor: pointer;
      list-style: none;
      user-select: none;
    }

    .roadmap-summary::-webkit-details-marker {
      display: none;
    }

    .roadmap-toggle-icon {
      display: inline-block;
      font-size: 1rem;
      color: var(--color-primary-600, #1E3A8A);
      flex-shrink: 0;
      transition: transform 250ms ease, color 200ms ease;
      margin-top: 4px;
      line-height: 1;
    }

    .roadmap-card[open] .roadmap-toggle-icon {
      transform: rotate(180deg);
      color: var(--color-primary-700, #131F5B);
    }

    .roadmap-content {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
      padding-top: var(--space-4);
      margin-top: var(--space-3);
      border-top: 1px solid var(--color-border-subtle, #e2e8f0);
    }
  }

  .roadmap-header {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    flex: 1;
  }

  .roadmap-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background-color: #1E3A8A;
    color: #ffffff;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.125rem;
    border-radius: var(--radius-md, 12px);
    flex-shrink: 0;
    letter-spacing: -0.01em;
  }

  .roadmap-title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0;
    line-height: 1.3;
    letter-spacing: -0.01em;
  }

  .roadmap-desc {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--color-gray-700);
    margin: 0;
  }

  .roadmap-finnish-term {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding-top: var(--space-2);
    margin-top: var(--space-2);
    border-top: 1px solid var(--color-border-subtle, #f1f5f9);
  }

  .term-label {
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-gray-500);
  }

  .term-text {
    font-family: var(--font-body);
    font-size: 0.875rem;
    font-style: italic;
    color: var(--color-gray-600);
  }

  /* ── Split Editorial: Document Checklist (4:3 Frame) ── */
  .editorial-split-layout {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: clamp(2.5rem, 5vw, 4.5rem);
    align-items: center;
  }

  .split-content-side {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-3);
  }

  .split-heading {
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 2.5vw, 2.1rem);
    font-weight: 700;
    color: var(--color-text-primary);
    line-height: 1.22;
    margin: 0;
  }

  .split-lead {
    font-family: var(--font-body);
    font-size: 0.96875rem;
    line-height: 1.6;
    color: var(--color-gray-700);
    margin: 0 0 var(--space-2);
  }

  .document-disclaimer-note {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    background-color: #eff6ff;
    border: 1px solid #bfdbfe;
    border-radius: var(--radius-md, 10px);
    padding: var(--space-4);
    margin-bottom: var(--space-2);
  }

  .disclaimer-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background-color: #1E3A8A;
    color: #ffffff;
    font-size: 0.875rem;
    flex-shrink: 0;
  }

  .disclaimer-text {
    font-family: var(--font-body);
    font-size: 0.875rem;
    line-height: 1.55;
    color: #1e40af;
    margin: 0;
  }

  .requirements-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    width: 100%;
  }

  .requirement-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    background-color: #f8fafc;
    border: 1px solid var(--color-border-subtle, #e2e8f0);
    border-radius: var(--radius-md, 10px);
    padding: var(--space-3) var(--space-4);
  }

  .req-check {
    color: var(--color-primary-600, #1E3A8A);
    font-weight: 800;
    font-size: 1.1rem;
    flex-shrink: 0;
  }

  .req-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .req-title {
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 700;
    color: #131F5B;
    margin: 0;
  }

  .req-detail {
    font-family: var(--font-body);
    font-size: 0.84375rem;
    line-height: 1.5;
    color: var(--color-gray-600);
    margin: 0;
  }

  .split-image-frame {
    position: relative;
    border-radius: var(--radius-lg, 16px);
    overflow: hidden;
    aspect-ratio: 4 / 3;
    box-shadow: 0 10px 28px -6px rgba(19, 31, 91, 0.12),
                0 0 0 1px rgba(19, 31, 91, 0.08);
    background-color: #f8fafc;
  }

  .split-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 450ms ease;
  }

  .split-image-frame:hover .split-img {
    transform: scale(1.03);
  }

  /* ── FAQs Accordion ── */
  .faqs-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    max-width: 860px;
    margin: 0 auto;
  }

  .faq-accordion-item {
    background: #ffffff;
    border: 1px solid var(--color-border-subtle, #e2e8f0);
    border-radius: var(--radius-lg, 14px);
    padding: var(--space-5) var(--space-6);
    transition: border-color 200ms ease, box-shadow 200ms ease, background-color 200ms ease;
  }

  .faq-accordion-item[open] {
    border-color: #93c5fd;
    box-shadow: 0 6px 20px -4px rgba(19, 31, 91, 0.08);
  }

  .faq-accordion-trigger {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    cursor: pointer;
    list-style: none;
    user-select: none;
  }

  .faq-accordion-trigger::-webkit-details-marker {
    display: none;
  }

  .faq-q-text {
    font-family: var(--font-display);
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--color-text-primary);
    line-height: 1.35;
    margin: 0;
    transition: color 200ms ease;
  }

  .faq-accordion-item[open] .faq-q-text {
    color: var(--color-primary-600, #1E3A8A);
  }

  .faq-accordion-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: var(--radius-sm, 6px);
    background-color: #f1f5f9;
    color: #131F5B;
    flex-shrink: 0;
    transition: transform 250ms ease, background-color 200ms ease, color 200ms ease;
  }

  .faq-accordion-item[open] .faq-accordion-icon {
    transform: rotate(180deg);
    background-color: #131F5B;
    color: #FFD26F;
  }

  .faq-accordion-body {
    padding-top: var(--space-3);
    margin-top: var(--space-3);
    border-top: 1px solid var(--color-border-subtle, #f1f5f9);
  }

  .faq-a-text {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--color-gray-700);
    margin: 0;
  }

  /* ── Handbook Spotlight Card ── */
  .handbook-spotlight-card {
    background: linear-gradient(145deg, #131F5B 0%, #0a1133 100%);
    border: 1.5px solid rgba(255, 210, 111, 0.35);
    border-radius: var(--radius-xl, 24px);
    padding: clamp(2rem, 3.5vw, 3.25rem);
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: clamp(2rem, 3.5vw, 3.25rem);
    align-items: center;
    box-shadow: 0 20px 50px -10px rgba(19, 31, 91, 0.4);
  }

  .handbook-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .handbook-badge-row {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  .handbook-pill {
    color: #FFD26F;
    font-family: var(--font-display);
    font-size: 0.78125rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .handbook-free-tag {
    display: inline-flex;
    align-items: center;
    font-family: var(--font-display);
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    color: #131F5B;
    background-color: #FFD26F;
    padding: 3px 10px;
    border-radius: 9999px;
  }

  .handbook-title {
    font-family: var(--font-display);
    font-size: clamp(1.65rem, 2.5vw, 2.25rem);
    font-weight: var(--font-weight-bold);
    color: #ffffff;
    line-height: 1.18;
    margin: 0;
  }

  .handbook-subtitle {
    font-family: var(--font-display);
    font-size: 0.96875rem;
    font-weight: 600;
    color: #FFD26F;
    margin: 0;
    line-height: 1.35;
  }

  .handbook-desc {
    font-family: var(--font-body);
    font-size: 0.90625rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.88);
    margin: 0;
  }

  .handbook-highlights-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    background-color: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: var(--radius-md, 12px);
    padding: var(--space-4);
  }

  .handbook-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-2);
    color: #ffffff;
    font-size: 0.84375rem;
    line-height: 1.45;
  }

  .handbook-check {
    color: #FFD26F;
    font-weight: 800;
  }

  .handbook-actions {
    margin-top: var(--space-2);
  }

  .btn-handbook-primary {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background-color: #FFD26F;
    color: #131F5B;
    border: 2px solid #FFD26F;
    font-family: var(--font-display);
    font-size: 0.9375rem;
    font-weight: 700;
    text-decoration: none;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-sm);
    box-shadow: 0 4px 14px rgba(255, 210, 111, 0.35);
    transition: background-color 200ms ease, color 200ms ease, border-color 200ms ease, transform 150ms ease;
  }

  .btn-handbook-primary:hover {
    background-color: #ffffff;
    border-color: #ffffff;
    color: #131F5B;
    transform: translateY(-2px);
  }

  .handbook-media-frame {
    position: relative;
    border-radius: var(--radius-lg, 16px);
    overflow: hidden;
    aspect-ratio: 4 / 3;
    border: 1px solid rgba(255, 210, 111, 0.25);
    box-shadow: 0 16px 36px rgba(0, 0, 0, 0.35);
  }

  .handbook-cover-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 500ms ease;
  }

  .handbook-media-frame:hover .handbook-cover-img {
    transform: scale(1.03);
  }

  /* ── Responsive Adaptations ── */
  /* Mobile Image with Overlay */
  .hero-image-mobile {
    display: none;
    width: 100%;
    position: relative;
    margin: var(--space-4) 0;
  }

  .hero-areas-list-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 10;
    background: linear-gradient(to top, rgba(19, 31, 91, 0.85), transparent);
    padding: var(--space-4) var(--space-3) var(--space-3);
    border-radius: 0 0 var(--radius-lg, 16px) var(--radius-lg, 16px);
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 6px;
  }

  .hero-areas-list-overlay .area-item {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: #ffffff;
    font-size: 0.625rem;
    font-weight: 400;
    padding: 2px 6px;
    backdrop-filter: blur(4px);
    text-align: center;
    line-height: 1.3;
    border-radius: var(--radius-sm);
  }

  /* ── Mobile Optimizations (≤1024px) ── */
  @media (max-width: 1024px) {
    /* Hero Layout */
    .service-hero-layout {
      grid-template-columns: 1fr;
      gap: var(--space-4);
      padding: var(--space-2) 0 var(--space-4);
    }

    .service-hero-content {
      align-items: center;
      text-align: center;
    }

    .service-hero-title {
      text-align: center;
    }

    .service-hero-lead {
      text-align: center;
      max-width: 100%;
    }

    /* Hide desktop image and regular chips */
    .hero-image-desktop {
      display: none;
    }

    .hero-areas-list {
      display: none;
    }

    /* Show mobile image with overlay */
    .hero-image-mobile {
      display: block;
    }

    /* Center section eyebrows */
    .section-eyebrow {
      justify-content: center;
      width: 100%;
    }

    /* Center CTAs */
    .service-hero-actions {
      justify-content: center;
      width: 100%;
    }

    /* Reduce section spacing */
    .section-header-center {
      margin-bottom: var(--space-4);
    }

    /* Authorities grid to single column */
    .authorities-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    /* Roadmap grid to single column */
    .roadmap-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .roadmap-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-2);
    }

    .roadmap-number {
      width: 40px;
      height: 40px;
      font-size: 1rem;
    }

    /* Editorial split to single column */
    .editorial-split-layout {
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }

    .split-visual-side {
      order: -1;
    }

    /* Handbook spotlight */
    .handbook-spotlight-card {
      grid-template-columns: 1fr;
      gap: var(--space-5);
      padding: var(--space-5);
    }

    .handbook-visual {
      order: -1;
    }

    /* FAQ grid to single column */
    .faqs-grid {
      max-width: 100%;
    }
  }

  /* ── 768px: Mobile ── */
  @media (max-width: 768px) {
    .service-hero-layout {
      padding: var(--space-6) 0;
    }

    .service-hero-title {
      font-size: clamp(1.75rem, 6vw, 2.25rem);
    }

    .service-hero-lead {
      font-size: 1rem;
    }

    .section-heading {
      font-size: clamp(1.5rem, 5.5vw, 1.875rem);
    }

    .section-intro {
      font-size: 0.9375rem;
      margin-bottom: var(--space-6);
    }

    .service-hero-actions {
      flex-direction: column;
      width: 100%;
      align-items: stretch;
    }

    .btn-primary-action,
    .btn-secondary-action {
      width: 100%;
      justify-content: center;
      min-height: 48px;
      text-align: center;
    }

    .authorities-grid {
      grid-template-columns: 1fr;
    }

    .roadmap-grid {
      grid-template-columns: 1fr;
    }

    .roadmap-number {
      width: 40px;
      height: 40px;
      font-size: 1rem;
    }

    .authority-card {
      padding: var(--space-5);
    }

    .roadmap-card {
      padding: var(--space-5);
    }

    .handbook-spotlight-card {
      grid-template-columns: 1fr;
      gap: var(--space-6);
      padding: var(--space-6);
    }

    .handbook-visual {
      order: -1;
    }

    .faq-accordion-item {
      padding: var(--space-4);
    }

    .faq-q-text {
      font-size: 0.9375rem;
    }

    .section-header-center {
      margin-bottom: var(--space-6);
    }
  }

  /* Mobile Image with Overlay */
  .hero-image-mobile {
    display: none;
    width: 100%;
    position: relative;
    margin: var(--space-4) 0;
  }

  .hero-areas-list-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 10;
    background: linear-gradient(to top, rgba(19, 31, 91, 0.85), transparent);
    padding: var(--space-4) var(--space-3) var(--space-3);
    border-radius: 0 0 var(--radius-lg, 16px) var(--radius-lg, 16px);
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 6px;
  }

  .hero-areas-list-overlay .area-item {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: #ffffff;
    font-size: 0.625rem;
    font-weight: 400;
    padding: 2px 6px;
    backdrop-filter: blur(4px);
    text-align: center;
    line-height: 1.3;
    border-radius: var(--radius-sm);
  }

  /* ── Mobile Optimizations (≤1024px) ── */
  @media (max-width: 1024px) {
    /* Hero Layout */
    .service-hero-layout {
      grid-template-columns: 1fr;
      gap: var(--space-4);
      padding: var(--space-2) 0 var(--space-4);
    }

    .service-hero-content {
      align-items: center;
      text-align: center;
    }

    .service-hero-title {
      text-align: center;
    }

    .service-hero-lead {
      text-align: center;
      max-width: 100%;
    }

    /* Hide desktop image and regular chips */
    .hero-image-desktop {
      display: none;
    }

    .hero-areas-list {
      display: none;
    }

    /* Show mobile image with overlay */
    .hero-image-mobile {
      display: block;
    }

    /* Center section eyebrows */
    .section-eyebrow {
      justify-content: center;
      width: 100%;
    }

    /* Center CTAs */
    .service-hero-actions {
      justify-content: center;
      width: 100%;
    }

    /* Reduce section spacing */
    .section-header-center {
      margin-bottom: var(--space-4);
    }

    /* Authorities grid to single column */
    .authorities-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    /* Roadmap grid to single column */
    .roadmap-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    /* Editorial split to single column */
    .editorial-split-layout {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    /* Checklist grid to single column */
    .checklist-grid {
      grid-template-columns: 1fr;
    }

    /* FAQ grid to single column */
    .recognition-faqs-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ── 480px: Small phones ── */
  @media (max-width: 480px) {
    .service-hero-title {
      font-size: 1.625rem;
      letter-spacing: -0.02em;
    }

    .breadcrumb-current {
      display: none;
    }

    .roadmap-step {
      padding: var(--space-4);
    }

    .step-number {
      font-size: 1.5rem;
    }

    .hero-areas-list-overlay .area-item {
      font-size: 0.5625rem;
      padding: 1px 4px;
    }
  }
</style>

<script>
  // Remove 'open' attribute on mobile for accordion behavior
  if (window.innerWidth <= 768) {
    document.querySelectorAll('.authority-card[open], .roadmap-card[open]').forEach((detail) => {
      detail.removeAttribute('open');
    });
  }
</script>
